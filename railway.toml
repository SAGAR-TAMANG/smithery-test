[build]
builder = "NIXPACKS"
# We are overriding the default install command to be more direct
installCommand = ". /opt/venv/bin/activate && uv pip install ."

[deploy]
startCommand = "fastmcp run main.py --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10